## Setup

1. Clone the repo
2. Create environment: `python -m venv air_canvas_env`
3. Activate: `air_canvas_env\Scripts\activate`
4. Download dataset manually ( `kagglehub.dataset_download("jkim289/handwritten-korean-characters")` ) or run Step 2 in the notebook,
